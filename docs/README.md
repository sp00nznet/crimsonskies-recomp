# Notes

Per-subsystem notes get written here as work happens. Nothing yet beyond what is in
the top-level README.md and docs/PROJECT-NOTES.md.

For the Microsoft BC recompiler teardown that shaped this project's toolchain, see
`xboxrecomp/docs/technical/ms-fusion-*.md`.
